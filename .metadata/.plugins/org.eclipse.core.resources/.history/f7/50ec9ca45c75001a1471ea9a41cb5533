<!DOCTYPE html>
<html>
<head>
<meta charset="EUC-KR">
<title>Insert title here</title>
	<script type="text/javascript">
	
	var num = 2.1234;
	
	//검사할 숫자중 가장 큰 값을 판단해서 반환
	var maxNum = Math.max(10,5,8,30);
	
	//검사할 숫자중 가장 작은 값을 판단해서 반환
	var minNum = Math.min(10,5,8,30);
	
	//num 변수의 각 2.1234에서 소수점 첫째자리를 반올림하여 정수 2를 반환
	var roundNum = Math.round(num);
	
	//0~1 사이의 랜덤값를 발생시켜 반환
	var rndNum = Math.random();
	
	//Math.PI는 원주율 상수값을 반환
	var piNum = Math.PI;
	
	//변수 num의 값 2.1234의 소수점 첫째 자리값을 무조건 올림 처리하여 정수 3을 반환
	var ceilNum = Math.ceil(num);
	
	document.write(maxNum,"<Br>");
	</script>
</head>
<body>

</body>
</html>