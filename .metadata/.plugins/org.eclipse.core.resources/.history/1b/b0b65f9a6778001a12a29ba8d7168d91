<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>

	<script type="text/javascript">
	/*
		Math.random()메소드를 이용하여 숫자0부터~10까지의 난수를 반환함.
		->Math.random()* 10
		
		Math.random()메소드를 이용하여 0부터 10까지 정수로만 난수를 반환하려면
		Math.floor()메소드를 이용한다
		-> Math.floor(Math.random()*11);
	
	
		참고 : Math.floor(숫자)는 소수점 첫째자리에서 무조건 내림해서 정수를 반환한다.
		Math.random()을 이용하여 난수 발생 시켜 원하는 구간의 정수의 값을 구하는 공식
		공식 -> Math.floor(Math.random()*(최대값 - 최소값) + 1) + 최소값;
		
		120 ~ 150 사이의 난수를 정수로만 발생시키려면?
		Math.floor(Math.random()*(150 - 120) + 1) + 120;
		Math.floor(Math.random()*31) + 120;
		
		예제 설명
		선택문을 사용하여 사용자가 입력한 가위, 바위, 보를 숫자로 변환시킨후 변수 tNum에 저장한다
		만일 사용자가 가위, 바위, 보 이외에 잘못된 다른 값을 입력했을 떄 alert()메소드를 호출해
		경고메시지창을 띄운다
		location객체의 reload()메소드를 호출해 현재 math_ob2.html를 재요청하여
		웹브라우저의 새로고침을 발생하게 하자
	*/
	document.write("<h1>컴퓨터의 랜덤으로 내는 가위, 바위, 보 맞추기 게임</h1>");
	var t =prompt("가위, 바위, 보 중 하나를 입력하세요", "가위");
	var tNum;
	switch (t) {
		case "가위":
			tNum = 1;
			break;
		case "바위":
			tNum = 2;
			break;
		case "보":
			tNum = 3;
			break;
		default:
			alert("잘못된 값을 입력하였습니다");
			location.reload(); // 새로고침
	}
	//컴퓨터가 1부터 3까지 난수 발생
	var com = Math.ceil(Math.random() * 3);
	
	//난수를 이용해 컴퓨터 가위 바위 보 이미지가 출력되도록 함
	document.write("<img src ='images/math_img_" + com + ".jpg' />");
	//사용자가 입력한 값과 컴퓨터가 낸 랜덤값이 일치하나 조건 검사함
	if(tNum == com){
		alert("맞췄습니다!");
	}
	else{
		alert("틀렸습니다!");
	}
	</script>
</head>
<body>
<img = "이미지파일의 경로"/>
</body>
</html>