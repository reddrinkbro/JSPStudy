<!DOCTYPE html>
<html>
<head>
<meta charset="EUC-KR">
<title>Insert title here</title>
	<script type="text/javascript">
		//예제: 배열 객체의 메소드 concat(),pop(),push(),shift(),unshift()를 사용한 예제
		//concat()메소드를 사용하여 2개의 배열 객체를 하나의 배열객체로 만들어 객체를 출력함
		//pop(),push(),shift(),unshift()메소드를 사용하여 배열 객체의 앞뒤에 저장된 데이터를 삭제하거나
		//새로운 데이터를 배열 메모리에 추가 함
		
		var greenLine = ["사당", "교대", "방배", "강남"];
		var yellowLien = ["미금", "정자", "모란", "수서"];
		
		//2개의 배열객체를 연결하여 하나의 배열객체로 만들어 반환
		var twoLine = greenLine.concat(yellowLien);
		document.write(twoLine,"<br/>");
		
		//pop()
		greenLine.pop();
		document.write(greenLine,"<br/>");
		
		greenLine.push("삼성");
		document.write(greenLine,"<br/>");
	</script>
</head>
<body>

</body>
</html>